fn fib(n) {
    if n <= 1 {
        return n
    }
    return fib(n-1) + fib(n-2)
}

const start = time.time() // time in seconds
const r = fib(25)
const end = time.time()
echo("Time taken: {} s".format(end - start))
echo("Result:", r)